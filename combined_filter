%full deconvoluton filter
%this constructs the filter using an ifft and the deconvolution and weiner
%parts

%loading components
Deconv_protocol; %deconv is the vairable needed
Weiner_protocol; %W is the variable needed

%multiplying the two variables together

deconv_W = deconv .* W;

%performing inverse fourier transform

deconv_filter = ifft(deconv_W);
